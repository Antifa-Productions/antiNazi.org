include:
  template: Jobs/Secret-Detection.latest.gitlab-ci.yml
  image: alpine:latest
##
pages:
  stage: deploy
  stage: test
  script:
  - echo 'Nothing to do...'
  artifacts:
    paths:
    - public
  only:
  - master